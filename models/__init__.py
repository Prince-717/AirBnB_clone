"""
To initialize the FileStorage module, create a new instance of FileStorage called "storage" and invoke the reload() method on this instance.
"""
from models.engine.file_storage import FileStorage


storage = FileStorage()
storage.reload()
